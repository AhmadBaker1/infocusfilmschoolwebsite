node_modules/
dist/
.astro/
.DS_Store
*.log

# Camera originals: ~79MB including a 48MB video and a 24MB RAW. Web
# derivatives live in public/. Track these with git-lfs if they need to
# be in the repo — committing them plainly bloats history permanently.
source-photos/

# Local env (backend endpoints etc.)
.env
.env.*

# Cloudflare: local secrets, local D1/R2 state, generated types
.dev.vars
.wrangler/
worker-configuration.d.ts
